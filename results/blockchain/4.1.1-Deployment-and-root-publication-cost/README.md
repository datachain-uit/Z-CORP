# Deployment and root-publication cost

<!-- pre-correction banner (rerun protocol v3, section 7.4) -->
> **Pre-correction campaign (July 2026) — superseded, not used by the CSI manuscript.**
> `20260712T134135-deploy-sepolia.csv` and `20260712T134140-deploy-zkSyncSepolia.csv` record the deployment
> used by the July on-chain campaign in `../4.1.2-Proof-verification-behavior-across-time-windows/`; it took place
> before the selector-booleanity correction of `circuits/CredentialVerifier.circom` (commit `2524431`, P4a). The file names record when the files were written; the transactions took place on 2026-07-11.
> Both files are kept unchanged as historical evidence. First commits, git blob IDs and sha256: [`results/PRECORRECTION-2026-07.sha256`](../../PRECORRECTION-2026-07.sha256).
> The two `20250430T...` files predate the July campaign and are outside the scope of that manifest.
> The post-correction on-chain rerun (protocol v3, section 5) has not been run yet.
